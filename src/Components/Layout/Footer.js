import React from 'react';
import { Layout } from 'antd';
function Footer() {
    return (
     <Layout.Footer  style={{ minHeight: 'auto' }}>
     </Layout.Footer>
    );
  }
  
  export default Footer;